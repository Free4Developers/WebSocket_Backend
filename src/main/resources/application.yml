spring:
  profiles:
    include: oauth

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true

jwt:
  secret-key: Y2g0bmp1bg== # ch4njun

debug: true